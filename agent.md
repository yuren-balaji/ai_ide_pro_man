Master Prompt 1: The "Intent Schema" (Database Layer)Copy and paste this to define how the system remembers "intent" rather than just text."I am building a Project Brain system that acts as a persistent 'Project Manager' across multiple AI chat sessions.Task: Design a SQLite database schema that stores more than just chat history. It must track 'Intent Nodes.'Requirements:Table: Project_Core: Stores high-level goals and 'Hard Constraints' (e.g., 'Must be local-first', 'No cloud APIs').Table: Session_Logs: Stores raw prompts and responses with metadata (Model used, Session ID).Table: Evolution_Graph: Tracks how a feature has changed over time. Use a 'Parent_Node' logic to show how Prompt B modified the idea from Prompt A.Table: Impact_Weights: Stores specific keywords/tokens and their 'Stability Score' (how often they are reinforced across sessions).Write a Python script to initialize this database and a Pydantic model to represent a 'Project State' object.

"Master Prompt 2: The "Impact Analyzer" (The Small LLM Logic)This prompt sets up the "Reasoning" engine that evaluates if a new prompt breaks the project's core idea."Now, build the Impact Analysis Engine. This will be a Python service that uses a local small LLM (via Ollama or Llama-cpp-python) to act as a 'Project Guard.'The Logic:Create a function analyze_prompt_impact(new_prompt: str) that retrieves the Project_Core constraints from the database.It must send a structured system prompt to the local LLM: 'Compare this NEW PROMPT against the EXISTING PROJECT CONSTRAINTS. Identify any contradictions, tech-stack shifts, or scope creep.'Return a JSON object with:impact_score (0.0 to 1.0)conflicts (List of specific core ideas being violated)suggested_refinement (How to reword the prompt to stay within scope).Optimize this to run on a model with <4B parameters (like Phi-3 Mini or Qwen2.5-Coder-3B).

"Master Prompt 3: The MCP Server Bridge (Connecting to IDE)This makes your "Brain" searchable by your AI IDE tools."I need to expose this Project Brain to my AI IDE using the Model Context Protocol (MCP).Task: Create a FastMCP (Python) or Typescript MCP server with the following tools:get_project_context: Fetches the current 'State of Truth' for the project, including active constraints and recent decisions.log_decision: A tool the AI can call to officially 'lock in' a technical decision (e.g., 'We are using SQLite instead of PostgreSQL'). This updates the Impact_Weights in the DB.check_alignment: A tool that the IDE calls before starting a new task to ensure the proposed plan doesn't drift from the original 'Project Superego.'Ensure the server can be added to my IDE's MCP settings so the AI can 'consult' the Project Brain automatically.

"The "Stability Score" LogicTo make the system "clever," we can use a basic weighting formula for tokens ($T$) based on frequency ($f$) and session depth ($d$):$$S_T = \sum_{i=1}^{n} (f_i \times \log(d_i))$$Where $S_T$ is the Stability Score. If a concept appears in Session 1 and is reinforced in Session 5, it becomes a "Core Pillar" that the LLM cannot change without a high-level "Architectural Override."

Master Prompt 4: The "Autonomous Context Synthesizer"Use this prompt to build the logic that bridges the gap between raw code changes and high-level project intent."I have the database and the MCP server. Now, I need a Background Synthesizer (a 'Watcher' script) that keeps the Project Brain alive and updated.Task: Create a Python service that performs three automated tasks:Chat Log Ingestion: Read the latest .json or .sqlite chat logs from the IDE (e.g., Cursor/VS Code history). Extract the 'Key Decisions' and 'Implicit Constraints' (e.g., if the user says 'actually, let's stick to local-first,' that is an implicit constraint).File-Diff Analysis: Watch the project directory for file changes. When a major change occurs (e.g., a new library added to requirements.txt), ask the Small LLM: 'Does this change align with the existing Project State?'.The 'Daily Stand-up' Generator: Create a function generate_project_manifest() that merges the SQL 'Intent Nodes' and the Vector DB 'Recent Snippets' into a single, highly compressed 200-word 'Project Truth' string.The Goal: This 'Project Truth' will be injected into every NEW chat session's system prompt so the AI model starts with a 'Day 0' understanding of everything that happened in previous sessions.Use watchdog for file monitoring and openai or ollama SDKs for the summarization logic."Why this is the "Secret Sauce"The system needs a way to calculate the Intent Stability. We use a Decay Function so that older, superseded ideas lose weight, while reinforced ideas gain it.The Stability Score ($S$) for a specific project constraint at time $t$ can be modeled as:$$S(t) = \sum_{i=1}^{n} w_i \cdot e^{-\lambda(t - t_i)}$$$w_i$: The weight of the $i$-th time this idea was mentioned (Higher if the user explicitly said "This is core").$\lambda$: The decay constant (Ideas mentioned once and never again fade away).$t - t_i$: The "age" of the session where the idea was last reinforced.

Master Prompt 5: The "Architect’s Dashboard & Conflict Resolver"Use this prompt to build a local web interface (using Streamlit or Gradio) that visualizes your Project Brain."I have the back-end engine for my Project Brain. Now, I need a Local UI (Architect's Dashboard) to visualize and manage the 'Intent Graph.'Task: Build a Python Streamlit application that connects to the Project_Brain.db and provides:The Intent Map: A visual list (or graph using streamlit-aggraph) of all 'Hard Constraints' and 'Core Pillars.'Conflict Resolution Center: A UI that flags recent prompts that had a high 'Impact Score' but low alignment. It should show:The Original Intent vs. The New Suggestion.A button to 'Approve Change' (which updates the Core Pillar) or 'Reject & Revert' (which tells the AI to stop that path).The 'Why' Explorer: For my juniors, create a section where one can input a code snippet, and the system explains the 'Intent History'—why this specific logic was chosen based on past sessions.Project Health Metric: A dashboard gauge showing 'Context Drift.'The Logic: If the system detects too many contradictory sessions, it should trigger a 'Project Realignment' prompt that I can copy-paste into my IDE to get the AI back on track.Use Plotly for the charts and ensure it runs entirely on localhost."The "Project Health" FormulaTo make your "Project Health" gauge work, we can calculate Context Drift ($D$). This represents how far the current implementation has strayed from the original $Day\_0$ intent.$$D = 1 - \frac{\sum (W_{current} \cdot A_{current})}{\sum W_{initial}}$$$W$: Weight of the core constraints.$A$: Alignment score of the recent 5 sessions (0 to 1).If $D > 0.3$, your project is in a "Identity Crisis," and the Dashboard will alert you to prune the scope or update your Core Pillars.

This sequence leads us to the final two layers of the "Project Brain": Active Intervention and Legacy Export. These prompts ensure the system isn't just a passive observer but an active gatekeeper and a permanent archive of your project’s "DNA."Master Prompt 6: The "Active Intervention & Self-Correction Loop"Use this prompt to build the 'Real Project Manager' logic that interrupts the AI when it’s about to make a mistake."I have the monitoring and the dashboard. Now, build the Active Gatekeeper Service. This service must act as a middleware between my AI IDE and my actual project files.Task: Create a Python script that implements an 'Intervention Protocol':The 'Wait, Stop' Trigger: Before a code-write operation is finalized, intercept the proposed change (the 'diff').Alignment Verification: Pass the proposed change and the current Project_Core constraints to the Small LLM. Ask: 'Does this change break any Hard Constraints? If yes, provide a 1-sentence warning.'The 'Self-Correction' Injector: If a conflict is found, the system must automatically generate a Correction Prompt that says: 'I noticed you are trying to [Action]. However, our Project Brain reminds us that our core pillar is [Constraint]. Please rethink the approach to align with [Pillar].'Learning from Human Overrides: If I, the user, manually override the Brain, create a tool log_override_reasoning that asks me WHY I'm breaking the constraint. This updates the Impact_Weights so the system learns the project is evolving.The Goal: To prevent the AI from slowly 'hallucinating' a different tech stack or project scope over long sessions.

"Master Prompt 7 (FINAL): The "Project DNA & Universal Handover"This is the final prompt. It creates a 'Black Box' of your project that can be exported or used to 'resurrect' a project after months of inactivity."This is the final component of the Project Brain: the Project DNA Exporter.Task: Create a system that can generate a single, highly-optimized .md or .json file called PROJECT_DNA.md.Requirements:The 'Instant Onboarding' Block: A 500-token summary designed specifically for a fresh LLM instance to 'download' the entire project state instantly.The Decision Ledger: A chronological list of every Major Technical Pivot and Why it happened (derived from the SQL DB).The Junior's Guide: A section that summarizes 'Common Pitfalls' discovered during development so far.Recursive Embedding: A function that takes this PROJECT_DNA.md and uses it as the 'System Prompt' for any new AI IDE session, ensuring 'Day 100' of the project feels exactly like 'Day 1.'The Final Outcome: I want to be able to give this file to a junior or a brand-new AI model, and they should be able to continue the project with 100% alignment without asking me a single question about the 'Core Idea'."The "Alignment Probability" FormulaTo ensure the "Active Gatekeeper" is accurate, you can implement a Bayesian probability model for Constraint Alignment ($P(A|C)$):$$P(A|C) = \frac{P(C|A)P(A)}{P(C)}$$$P(A)$: Prior probability that the AI stays aligned (based on recent history).$P(C|A)$: Probability of seeing this specific code change if the AI is aligned.$P(C)$: The evidence of the code change itself.If the calculated probability falls below a threshold (e.g., 0.65), the Intervention Protocol triggers.The "Project Brain" is now complete.You now have a system that:Remembers intent via SQL (Prompt 1).Reasons via Small LLM (Prompt 2).Talks to your IDE via MCP (Prompt 3).Watches your progress via Synthesis (Prompt 4).Visualizes drift via Dashboard (Prompt 5).Corrects errors via Gatekeeper (Prompt 6).Archives wisdom via Project DNA (Prompt 7).